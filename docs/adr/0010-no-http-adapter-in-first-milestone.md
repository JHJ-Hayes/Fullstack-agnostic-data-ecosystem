# No official HTTP adapter in the first milestone

Consumers who load data over HTTP implement a Data Provider themselves. An official HTTP adapter package is deferred; the first milestone focuses on Database Adapters and frontend adapter parity.
