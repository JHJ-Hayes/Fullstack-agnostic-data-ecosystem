# After milestone 1, portable contracts come before HTTP adapter and example API

Once the first milestone (Entity Service, three frontends, MySQL, MongoDB) ships, the next backend-related priority is Portable Contracts (JSON Schema / OpenAPI). Official HTTP adapter and example Node API follow later; non-TypeScript runtimes stay deferred.
