# Frontend adapter parity is semantic, not identical names

React, Vue, and Angular adapters must share the same capabilities: inject an Entity Service, expose a generic entity bridge, ship a User example API, and surface AsyncState. Framework-idiomatic naming (e.g. provideFae) is allowed; lockstep export identifiers are not required.
