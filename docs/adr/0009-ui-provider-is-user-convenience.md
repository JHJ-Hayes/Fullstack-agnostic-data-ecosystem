# UI Service Provider is a User convenience entry, not an entity registry

Frontend Service Providers (e.g. FaeProvider) inject the default User-oriented service for convenience. Custom Entities are passed explicitly into the generic entity bridge. A multi-Entity registry is out of scope for the first milestone.
